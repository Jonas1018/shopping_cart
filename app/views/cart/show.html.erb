<%= turbo_frame_tag "cart" do %>
  <h1>Cart</h1>
  <% if @cart.orders.count < 1 %>
    <p>Your cart is empty</p>
  <% else %>
    <%= render "cart/orders" %>
    <p>Total: <%= @cart.total %></p>
  <% end %>
<% end %>